intents:
  - askextension_tomato:
      triggers: respond_askextension_tomato

actions:
  - respond_askextension_tomato
   
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
